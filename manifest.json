{

  "manifest_version": 2,
  "name": "BGP.tools Search",
  "version": "0.1",

  "description": "Adds a context menu option to search a selected ASN or IP address with BGP.tools",
  
  "background": {
	  "scripts": ["index.js"]
  },
  
  "permissions": [
    "activeTab",
    "contextMenus"
  ]
}